notests *default:
  build:
    target: Vostok.System.Metrics.Windows.sln
    configuration: NoTests
    tool:
      name: dotnet

  install:
    - Vostok.System.Metrics.Windows/bin/Release/netstandard2.0/Vostok.System.Metrics.Windows.dll

  deps:
    - vostok.devtools

full-build > notests:
  build:
    target: Vostok.System.Metrics.Windows.sln
    configuration: Release